<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Training Tracker', root_path, class: 'navbar-brand' %>
    </div><!--/.navbar-header -->

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        <% if user_signed_in? %>
          <li class="<%= 'active' if request.path =~ /\/workouts/ %>"><%= link_to t('.workouts'), workouts_path %></li>
          <li class="<%= 'active' if request.path =~ /\/statistics/ %>"><%= link_to t('.statistics'), '#TODO', onclick: "alert('#{t(:not_available_yet)}'); return false" %></li>
          <li class="<%= 'active' if request.path =~ /\/coaches/ %>"><%= link_to t('.coaches'), '#TODO', onclick: "alert('#{t(:not_available_yet)}'); return false" %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="<%= 'active' if request.path == edit_user_path %>">
            <%= link_to t('.my_profile'), edit_user_path %>
          </li>
          <li><%= link_to t('.sign_out'), logout_path %></li>
          <li>
            <%= link_to current_user.strava_profile_url, target: '_blank', class: 'navbar-brand' do %>
              <%= image_tag(current_user.strava_photo_url(:medium), class: 'img-responsive img-circle', width: 24, height: 24) %>
            <% end %>
          </li>
        <% else %>
          <li><%= link_to t('.sign_in'), strava_login_path %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->

  </div>
</nav>
